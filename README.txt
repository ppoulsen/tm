Submitted Files:
 - proj2.c
   Contains the main function which reads in lines from stdin and uses tm.h functions to test test string.
 - tm.h
   Contains the public functions for the Turing Machine
 - tm.c
   Contains the logic and private functions for building and executing the Turing Machine
 - Makefile
   Tool for compiling to the proj2 executable.

Compilation:
 Run `make` from source directory.

Execution:
 ./proj2 < q1.txt
